/** Minimum viewport width to apply the desktop web layout. */
export const DESKTOP_WEB_MIN_WIDTH = 768;

/** Fixed width of the desktop web sidebar. */
export const DESKTOP_SIDEBAR_WIDTH = 240;

/** Max width of page content within the main area on desktop web. */
export const DESKTOP_MAIN_CONTENT_MAX_WIDTH = 900;

/** Max width of the add-period card on desktop web. */
export const DESKTOP_ADD_PERIOD_MAX_WIDTH = 560;

/** Background of the main content area on desktop web. */
export const DESKTOP_MAIN_BACKGROUND = '#f8f8f8';

/** Sidebar border color on desktop web. */
export const DESKTOP_SIDEBAR_BORDER = '#e5e5e5';
